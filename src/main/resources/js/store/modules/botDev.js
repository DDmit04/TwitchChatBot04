const state = {
    isDevMode: isDevMode,
    botDevUsername: botDevUsername,
    botDevOauth: botDevOauth,
    botDevChannels: botDevChannels
}
const getters = {}
const mutations = {}
const actions = {}

export default {
    namespaced: true,
    state,
    getters,
    actions,
    mutations
}